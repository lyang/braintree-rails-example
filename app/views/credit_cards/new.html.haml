- model_class = BraintreeRails::CreditCard
.page-header
  %h1="New #{model_class.model_name.human}"
= render :partial => "form", :locals => {:url => user_customer_credit_cards_path(@user)}

- content_for :javascript do
  :javascript
    var braintree = Braintree.create("#{ENV["CLIENT_SIDE_ENCRYPTION_KEY"]}")
    braintree.onSubmitEncryptForm('braintree-credit-card-form');
